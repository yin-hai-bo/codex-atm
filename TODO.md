# TODO List

- 支持多语言。
- 归档线程删除与 `state_5.sqlite` 中 `threads` 元数据的一致性问题尚未决策：
  删除归档文件但不更新 `state_5.sqlite` 会留下孤儿记录；删除文件时同步删除 `threads` 记录，又会导致用户从回收站恢复 `.jsonl` 文件后无法恢复对应标题等 metadata。需要明确最终策略。
- 右上角关闭按钮应始终可见，不应只在鼠标移到附近时才明显显示；同时它应处于最顶层，避免被其他界面元素遮挡。
- APP 应支持 `Alt+F4` 关闭主窗口。
- 主窗口尺寸需要再略微缩小，避免在 `1920x1080` 分辨率下出现被屏幕边缘遮挡的问题。
- Task Bar 图标显示仍有问题，需要继续确认是窗口配置问题还是 Windows 图标缓存问题。
